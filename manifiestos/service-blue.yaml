apiVersion: v1
kind: Service
metadata:
  name: orders-service-blue
  labels:
    app: orders
    version: blue
spec:
  type: LoadBalancer
  selector:
    app: orders
    version: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
