apiVersion: v1
kind: Service
metadata:
  name: orders-service
  labels:
    app: orders
spec:
  type: LoadBalancer
  selector:
    app: orders
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
